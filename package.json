{
  "name": "alexandersumer-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.17.0",
  "scripts": {
    "build": "pnpm --filter alexander-blog run build",
    "dev": "pnpm --filter alexander-blog run dev",
    "lint": "pnpm -r lint",
    "test": "pnpm --filter alexander-blog run test"
  },
  "engines": {
    "node": ">=20.10.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
